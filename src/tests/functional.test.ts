test("Should allow the user to upload a document", () => {
  //
});

test("Should send the user an email to confirm the document upload", () => {
  //
});

test("Should allow the recruiter to send a follow up request or a rejection email", () => {
  //
});
