
describe("Document", () => {
    test("Should return 200 and save the document", () => {
        //
    });

    test("Should return 500 if an unexpected error occurs while saving the document's information", () => {
        //
    });
});
