// Create a series of classes to extend the traditional document class

describe("Email", () => {
  test("Should return 200 and send an email", () => {
    //
  });

  test("Should return 500 if unable to send an email", () => {
    //
  });
});
